# 학습 결과물 정리표

| 항목 | 내용 |
|---|---|
| 제작자 | 공업생산팀, 박현재 |
| 명칭 | AI 기반 실물재고·전산재고 차이 분석 및 이상품목 관리 TOOL |
| URL | 없음 — 로컬 실행 (`inventory-dashboard.html`, `index.html`과 동일 내용) |
| 주요기능 | ① 카테고리별 전산재고 vs 실물재고 비교(막대차트), 재고 금액 편차(발산형 막대차트) ② 최근 6개월 재고 정확도 추이(라인차트) ③ 품목별 상세 비교 표(정렬·상태 필터·검색) — **미완성:** 최신월(2026-09) 외 5개월은 추이 차트만 표시되고 품목 상세는 "준비중" 상태, 실제 데이터 연동 전 |
| 사용대상 | 매월 실물재고 조사와 전산재고 대조 업무를 하는 담당자(재고관리/생산관리 부서로 추정, 확인 필요) |
| 비고 | **제작:** Claude Code로 제작(대화 내 확인). 개발기술: 순수 HTML/CSS/JS(바닐라, 외부 JS 라이브러리 없음), 차트는 SVG 직접 렌더링, 폰트만 Google Fonts CDN 사용. 데이터 저장: 별도 DB 없음 — 품목 19건과 6개월 추이 수치가 JS 코드 안에 **하드코딩된 예시(mock) 데이터**임(PRD상 실제 전산재고 시스템·실사 엑셀 데이터는 아직 미연동, 구조도 미확정). <br>**실행 방법:** `inventory-dashboard.html` 파일을 브라우저로 열면 바로 실행(별도 설치·서버 불필요). 인터넷 연결은 구글 폰트 로딩에만 필요, 그 외 외부 서비스 연동 없음. <br>**서비스 자체의 AI 사용:** 현재 코드상으로는 확인되는 AI 기능 없음(차이 계산·상태 판정은 고정 규칙(±2%) 기반, 순수 정적 계산/렌더링) — 명칭에 "AI 기반"이 포함되어 있으므로, 실제 AI 분석·이상탐지 기능을 추가할지 여부는 코칭 시 확인 필요. <br>**남은 문제·다음 작업:** 전산재고 시스템 추출본 및 실사 엑셀 파일의 실제 구조 확인 후 mock 데이터를 실데이터로 교체 필요, 과거 5개월분 품목 상세 데이터 연동 필요. 코칭 시 확인 질문 — 전산재고 시스템에서 데이터를 어떤 형식(API/엑셀 등)으로 뽑을 수 있는지, 실사 엑셀 양식이 매월 동일한지, 이 화면을 사내에 공유/배포해도 되는지(승인 필요). <br>**보안·공유 확인 사항:** 현재 화면 수치는 예시 데이터이지만, 실데이터 연동 시 품목코드·수량·단가 등 사내 재고 정보가 클라이언트 사이드 코드에 그대로 노출되는 구조이므로 외부 공개 전 접근 제한(사내망 한정 등) 필요. `.vercel` 폴더는 배포 인증 정보를 포함할 수 있어 git에 커밋되지 않도록 `.gitignore` 상태 유지 확인 필요. **본 문서 공유는 별도 승인 필요(임의 판단하지 않음).** |

---
*이 표는 코드(`inventory-dashboard.html`, `IDEAS.md`, `PRD-재고비교.md`, `.vercel/project.json` 등 비민감 메타데이터)와 대화 내용만으로 작성되었으며, 실제 업무 데이터나 비밀 설정 파일 내용은 열람하지 않았습니다.*
